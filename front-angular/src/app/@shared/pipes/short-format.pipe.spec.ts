import { ShortFormatPipe } from './short-format.pipe';

describe('ShortFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new ShortFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
